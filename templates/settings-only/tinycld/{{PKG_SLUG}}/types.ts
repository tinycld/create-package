// This package contributes only a settings panel — no pbtsdb collections,
// no schema extension. Keep this file for public type exports if any
// other package ever needs to import from {{PKG_SLUG}}.

export {}
